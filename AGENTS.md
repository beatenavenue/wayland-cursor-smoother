# 基本ルール
- 日本語で会話を行う
- 可能な限り内部思考の表示も日本語で行う
- ローカルファイル検索は本リポジトリ以下の階層のみを許可
  - ループバックマウントやネットワークマウントが多用されており、検索が終わらなくなるためである
  - 既に対象のパスが明確で検索せずにアクセスできる場合はアクセスしてよい
- Web検索については確認を求めず全て実行してよい
  - curlやwgetなどコマンドラインツールを用いる場合も検索の場合は許可は不要である
  - ログインや投稿が必要な場合はユーザの許可なく行ってはならない

# 作業の目的
## 解決すべき課題
KDE Plasma 5 / Wayland 環境において、マルチディスプレイ構成でディスプレイの配置が非矩形になる場合に、マウスカーソルがディスプレイの隅で引っかかる問題がある。

## どのように解決されるべきか
マウスカーソルがエリアの角に引っかからないようにする実装は Windows 11 には存在する。
この機能は Windows 11 において `Ease cursor movement between displays` (日本語では「ディスプレイ間でカーソルを簡単に移動させる」) として知られている。
詳細な内部ロジックは不明ながら `HKCU\Control Panel\Cursors\CursorDeadzoneJumpingSetting` というレジストリ名からも分かるように、カーソルが引っかかりのあるエリア端に接触した際に次のエリアにジャンプさせるのだろう。

これに相当する機能は Linux デスクトップ環境では存在せず、とくに Wayland 環境では Synergy/Barrier のようなソフトウェアも動作しないことからマウス操作のプログラムでの制御は難しそうだ。

## 本問題と混同しやすい機能
KDE Plasma 5 には KWin の設定で `Screen Edges > Edge barrier` (日本語では「画面の端 > エッジバリア」) という機能が存在する。
これはマルチディスプレイ構成において、ディスプレイの端でマウスカーソルの移動を**意図的に**妨げる機能である。
本機能はデフォルトで有効化されており、マルチディスプレイ構成でディスプレイの端にカーソルが近づくと、カーソルの移動が妨げられ、引っかかるように感じられる場合がある。
この機能は本来の目的が異なるため、本問題とは逆の効果をもたらす。

本問題は、そもそも非矩形のデスクトップ領域によって角ができてしまいそこにカーソルが引っかかることが問題となる。
エッジバリアは完全に矩形の配置であってもディスプレイ間の移動時にバリアを設ける機能であり、本問題と似たような現象を起こせるが本質的には関係ない。

## この問題に関するKDEに対するissueやPR
- 調査結果（要約）:
  - 現時点で、KWinやKDEの公式バグトラッカー上に「Windows 11 の `Ease cursor movement between displays` と同等の機能を追加してほしい」という明確なissue/PRは見つかりませんでした。
  - 代わりに、ユーザコミュニティ（KDE Discuss、Reddit など）で同様の要望や使い勝手改善の議論が複数見つかります。代表的なスレッドは下記参照。
  - GNOME側（mutter）や他のデスクトップでもマルチモニタ間のカーソル遷移に関する議論やissueが存在します（例: mutter issue #2029）。
- 代表的なコミュニティ／関連議論（例）:
  - Move mouse to Screen — https://discuss.kde.org/t/move-mouse-to-screen/28971
  - Looking for "Ease cursor movement between displays" (Reddit) — https://www.reddit.com/r/kde/comments/195jx5p/looking_for_ease_cursor_movement_between_displays/
  - GNOME/mutter issue（関連） — https://gitlab.gnome.org/GNOME/mutter/-/issues/2029
- 注記: 公式のfeature-requestが見つからない場合は、要望がバグとして上がっていない、または議論がフォーラム等に散在している可能性があります。今後は既存のコミュニティスレッドを参照して KWin の feature-request を新規に提案することを推奨します。

## この問題を既に解決しているツール
- Windows 11: `Ease cursor movement between displays`（レジストリ名: `HKCU\\Control Panel\\Cursors\\CursorDeadzoneJumpingSetting`）
  - 概要: Windows に組み込まれた機能で、非矩形配置などで角に引っかかる際にカーソルの遷移を補助（ジャンプ等）する振る舞いを提供する。
- Linux / Wayland 環境:
  - 結論: 標準的に同等の機能を提供する共通ツールや設定は見当たりません。公式のfeature-requestも確認できなかったため、ネイティブ対応は現状未実装と考えられます。
  - 代替案／回避策（限定的）:
    - コミュニティスクリプト（KWinスクリプト等）で特定条件下にカーソルを移動させるワークアラウンドを組む。
    - ディスプレイ配置・スケーリングの調整で問題を軽減する（根本解決ではない）。
    - 上流（KWin/mutter等）へ機能要望を提出し、ネイティブ実装を促す（推奨アクション）。
  - 補足: Synergy/Barrier といったツールはWaylandでは制約が大きく、本問題の解決には向かないことが多い。

# wayland-cursor-smootherについて
## 本プロジェクトでのアプローチ
### 必要機能
最低限必要な技術要素は以下である。

- マウスカーソルの座標を取得する機能
- マウスカーソルの座標を設定する機能

これらは相対座標ではなく絶対座標で取得/設定できなければならない。
またこれらはXであれば容易だがWayland環境で実現可能な手段が現状乏しいため特に注意して検討する必要がある。

### 深く検討しない機能
引っかかるスクリーン端が具体的にどこであるかを特定/設定する機能はあまり考えないこととする。
それらは動的に変化する要素ではなく、ユーザが座標リストを定義して利用するかたちにしてよいと考える。

### 当初の想定(実現可能性に疑問あり)
READMEで記述していたとおり

- `Cursor Feed`（KWin スクリプト）: ポインタ位置を D-Bus 経由で公開する KWin スクリプト。`~/.local/share/kwin/scripts/cursor-feed/` 配下に配置し、有効化して使う。
- `glide_cursor`（Python ヘルパ）: `Cursor Feed` が公開する位置情報を購読し、カーソルがディスプレイ端で移動を妨げられたと判断した場合に隣接スクリーンへ“滑らせる”操作を行う。

この2つのコンポーネントで計画を立てていた。
実際に実装を試みた点ではKWinでのカーソル位置取得とDBus配信はどうやら動作していないように見えた。
カーソル位置の設定は `ydotool mousemove` を用いることを想定していたがまだ試していない。

### 以前の検討で除外した手段
#### libei/xdg-desktop-portal-RemoteDesktop
画面共有とリモート操作用ライブラリ、これを使えばほぼ確実にカーソル位置の取得と設定が可能になる。
ただし画面共有中の警告表示が常に出ることとなり、実用的ではない。

## 実現可能性の再検証
### カーソル座標を取得する方法
以前の調査ではKWinスクリプトが唯一の手段であると判定していたが、他の手段もないか再検証する。

### カーソル座標を設定する方法
もし座標取得がKWinスクリプトであるなら設定もKWinスクリプトで行えたら通信のムダがない。
KWinスクリプトでは `workspace.cursorPos` で取得だけでなく設定もできるらしい。

ほかの手段としては現時点ではuinputという仮想ポインタデバイスによるものが現実的と思われる。
簡易に実現するにはydotooldがあるのだがこれは絶対座標を指定してもArea rectangleがn/aとなっており動作しない。
python-evdevを使ってuinput仮想デバイスを作ることでこの目的は実現できた。起動にはsudoが必要になるが常駐させるなら問題ないだろう。
